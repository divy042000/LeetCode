class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> another_vector;
     for(auto i=0;i<nums.size()/2;i++)
     {another_vector.push_back(nums[i]);
     another_vector.push_back(nums[i+n]);}
        return another_vector;
        
    }
};
